import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // FocusFlow requires Firebase to initialise — placeholder test
    expect(true, isTrue);
  });
}
